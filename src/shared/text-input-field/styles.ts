import {StyleSheet} from 'react-native';
import {hp} from 'utils';

export const styles = StyleSheet.create({
  input: {height: 40, paddingHorizontal: 10},
  clickable: {
    height: 40,
    paddingHorizontal: 10,
    justifyContent: 'center',
  },
  container: {
    borderBottomWidth: 1,
    marginBottom: hp(5),
  },
});
