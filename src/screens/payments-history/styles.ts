import {StyleSheet} from 'react-native';
import {hp} from 'utils';

export const styles = StyleSheet.create({
  empty: {
    textAlign: 'center',
    fontSize: hp(16),
    marginTop: hp(20),
  },
  container: {
    flex: 1,
    paddingTop: hp(70),
  },
});
